# GMGN App Phase 3 執行規劃（顧問覆核）

> 輸入：`gmgn-app-full-manual.md`（1281 行，Phase 1 實機手冊，全文已重讀）、`gmgn-website-plan.md`（532 行，Phase 2 DeepSeek 顧問分析＋CC 驗收，全文已重讀）、`index.html` 現有 GMGN 教學區塊（`id="tool-gmgn"`，428~720 行，逐段核對）
> 對照範本：`dex-screener-website-plan.md` 的分級與收斂方式
> 目的：GMGN 這條線的 Phase 1（探索）與 Phase 2（顧問分析）都已完成且未動過 `index.html`，本文件是覆核後、可以直接交給 Opus agent 執行的 **Phase 3 第一批**修改清單

---

## 先講結論

`gmgn-website-plan.md` 的分析品質很高，逐項核對手冊原文後，**分級判斷基本同意，沒有發現錯判或無依據的項目**——這份 DeepSeek 產出的規劃比我原本預期的更扎實：每個 🟢 項目都能在手冊裡找到對應行號，官方原文（閃電模式警語、審計免責聲明等級的東西）都有逐字抄錄，且已經自己標好「哪些是未驗證、不要寫死」。

但覆核後有一個判斷上的分歧，這也是本文件存在的理由：**DeepSeek 把 P0（🟢）九個模組＋兩處修正全部列為「這輪該做」，合計約 +2,500 字**，這個量級遠超過 DEX Screener 那條線實際跑過的任何一輪（三輪合計 A~E 只動了 7 個小點、每輪淨增 150~250 字）。`HANDOFF.md` 記錄的「內容偏長」問題不是理論風險，是這個站已經在犯的病；DEX Screener 那三輪之所以每次只挑 2~5 個小修改，就是刻意不讓一次 commit 把頁面撐太長。GMGN 的手冊材料比 DEX Screener 更豐富，但不代表要一次寫完——**本規劃只挑「這輪」要做的部分，其餘留在 `gmgn-website-plan.md` 當待辦**，符合它自己文件裡「可裁切順序」建議的第一批，但比它的第一批（A/B/C/E/F/M/N，約 1,090 字）再收斂一次。

判斷標準沿用 DEX Screener 三輪的紀律：只挑「讀者對著 App 找不到東西」（介面落差）或「App 顯示的東西看起來像安全訊號、實測發現不可信」（DEX Screener 那條線這次的 Audit 投票發現同類型）兩種等級的落差，其餘留給後續輪次。

---

## 覆核後的分級表（與 DeepSeek 原分級對照）

| 等級 | 項目 | 覆核結果 |
|---|---|---|
| 🟢🔥 **本輪必做・最高優先** | Step 3「三個主頁面」與 App 實際 5 個底部分頁不符（手冊第 2 章 vs `index.html:489-494`） | **同意 DeepSeek 的 P0 判斷，且比照 DEX Screener 第二輪「Step 2 首頁四大區塊」錯誤描述的處理原則——這是同一等級的問題：讀者照著文字在 App 上找不到「Trenches／Trending／Copy Trade」三個並列頁面，因為 App 是 5 個底部 tab，Trending 只是「發現」裡的子分頁，Copy Trade 是「跟單」tab。核對手冊 2 章的 tap 座標表與 3.0/6.0 節，描述準確，可直接採用** |
| 🟢🔥 **本輪必做・最高優先** | KOL 可信度／訊號牆案例（手冊 5.2.4／5.4／6.4，DeepSeek 的 M8＋N） | **同意，且是本輪對應 DEX Screener「Audit 投票不可信」發現的同型案例**：天眼訊號「3 KOL 淨流入」實測點開發現 3 人持倉都已是 $0（手冊 5.4 逐字核對無誤）；喊單榜 #1「207.52x 最佳倍數」實測只有 3 次喊單、其中一則現在 -92.48%（手冊 6.4 核對無誤）。這兩個案例都是**官方 App 自己的數字被拆穿**，跟 DEX Screener 那次「官方自己承認 Audit 資訊不保證準確」是同一類型的最高價值發現，優先度應該比 DeepSeek 排的位置（M8 排在🟢清單中段）更靠前 |
| 🟢 本輪必做 | Step 1 鏈清單只寫 5 條，實測 12 條＋Tron WebView（手冊 3.3，DeepSeek 的 P1-1） | 同意收錄，但**更正優先級**：DeepSeek 把它歸在 P1（🟡），我覆核後認為這跟 DEX Screener 那條線「一句話補充」等級的小修正（例如鏈別打字搜尋）性質相同，字數極小（60 字，不需圖），順手做掉的成本幾乎為零，沒有理由留到下一輪 |
| 🟡 下一輪 | 閃電模式警語＋五種賣出設置（手冊 6.1.1／6.1.3，DeepSeek 的 M7） | 同意🟢分級與內容，但字數較大（+380 字）且是擴充既有手風琴而非修正錯誤，優先度低於上面三項，留給下一輪 |
| 🟡 下一輪 | 退租（手冊 7.5.1，DeepSeek 的 M2） | 同意🟢分級，**但 DeepSeek 自己在「給下一手的提醒」裡寫了這項未實際執行、也沒在網頁版複核**——這類「未驗證」項目照專案慣例（DEX Screener 對「All DEXes 完整清單」「Rank vs Sort 是否重複」的處理方式）應該先標記，等有機會在網頁版或真機補測過一次再寫，不適合排進本輪 |
| 🟡 下一輪 | 釣魚偵測＋利潤分佈（手冊 7.7，DeepSeek 的 M3） | 同意🟢分級與內容準確度，但這是「新增一張風險卡片」而不是修正既有錯誤或補一句話，量級（+260 字）留給下一輪比較符合小批次節奏 |
| 🟡 下一輪 | 幣不見了／多錢包合併（M4）、戰壕反 rug 三條件（M5）、持有者分頁判讀（M6）、詞彙表 10 則（M9） | 同意 DeepSeek 的🟢分級與內容，皆為紮實、有依據的模組，但單項字數都在 250~1000 字之間，留給後續輪次 |
| 🟡🔴 維持 DeepSeek 原判 | P1 清單其餘 9 項、P2 全部 | 覆核後同意維持原分級，不調整 |

---

## 本輪（Round 1）具體修改清單

> 精神對照 DEX Screener 第二、三輪：只做「修正介面落差」＋「一個最高價值的信任拆解案例」，合計約 4 處、預估 **+330 字**，不新增任何 SVG 示意圖（沿用既有圖或不需圖），量級跟 DEX Screener 單輪相當。

| # | 位置 | 修改類型 | 摘要 | 預估新增字數 | 需配圖 |
|---|---|---|---|---|---|
| P3-1 | 🚀上手／Step 3（`index.html` 約 489~494 行） | **修正**既有錯誤描述 | 把「先看懂三個主頁面（Trenches／Trending／Copy Trade）」改寫成符合 App 實際介面的敘述：底部 5 個分頁（發現／戰壕／監控／跟單／資產），Trending 是「發現」裡的子分頁、Copy Trade 是「跟單」分頁；新手先從「發現」熱門榜開始看，戰壕與跟單等看懂安全面板再碰 | 改寫約 90 字，淨增約 40 字 | 不需（沿用既有 fig-10，不改圖，圖仍可代表戰壕三欄卡片流） |
| P3-2 | 🚀上手／Step 1（`index.html` 約 456 行） | 補一句 | 鏈清單從「BSC／ETH／Base／Monad／TRON」5 條更新為「實測共 12 條鏈（含 Robinhood／HyperEVM／X Layer／Arbitrum 等），Tron 是內嵌網頁模式、操作手感不同」；順帶回答「熱門榜怎麼都是沒看過的幣」＝停在別條鏈視角 | 約 70 字 | 不需 |
| P3-3 | 🟣進階／新增手風琴（放在既有「Sniper Bot」之後、「Telegram Alert Bot」之前） | 新增（本輪唯一新區塊） | 「KOL 訊號別急著信」：① 天眼訊號「3 KOL 淨流入」實測點開發現這 3 人持倉都已是 $0，訊號是「他們買過」不是「他們還在」；② 喊單榜「最佳倍數 207.52x」實測只有 3 次喊單撐出來，其中一則現在 -92.48%，看平均倍數前先看樣本數 | 約 160 字 | 不需 |
| P3-4 | ⚠️風險／既有 callout 或自檢清單末尾 | 補一句 | 「App 內的『淨流入』『平均倍數』等訊號數字，官方自己的資料點開來看常常跟表面數字不一樣——跟 DEX Screener 教學提過的『Audit 投票數字可被刷』是同一個道理：工具給的摘要指標，永遠要點進去看明細再決定」 | 約 60 字 | 不需 |

**本輪合計約 +330 字**，其中 P3-1 是修正（不完全是淨增），P3-3 是本輪唯一的新增區塊。四處都不需要新畫 SVG，執行風險最低。

---

## 本輪明確排除清單（不是不好，是留給後續輪次）

- **M2 退租**：DeepSeek 自己標記「未實際執行、未在網頁版複核」，等有人力補測後再排入，避免把未經自己驗證過的操作路徑寫進網站（呼應 DEX Screener 對「All DEXes 清單」「Rank vs Sort」的處理方式）。
- **M3 釣魚偵測＋利潤分佈**：內容紮實但屬新增卡片、非修正錯誤，量級留給下一輪。
- **M4 幣不見了＋多錢包合併、M5 戰壕反 rug 三條件、M6 持有者分頁判讀、M7 Copy Trade 表單補丁（閃電模式／五種賣出）**：皆為 DeepSeek 覆核無誤、有價值的模組，但單項字數大（250~380 字），留給第二、三輪，執行方式可比照本輪直接照抄 `gmgn-website-plan.md` 2.2 節對應模組的內容大綱。
- **M9 詞彙表 10 則新詞**：機械性工作但涉及 `window.GLOSSARY` 陣列與 `data-term` 精確比對（`gmgn-website-plan.md`「給下一手的提醒」已列出三個技術硬規則），建議獨立一輪執行並找機會做完整驗證，不與內容輪次混在一起。
- **P1 清單其餘 9 項（圖表頭像標記、推特監控只看 CA、榜單三案例、天眼跟單、通知門檻、冷啟動畫面、託管錢包安全觀念、提現位置、早晚報／盈虧日曆）**：維持 DeepSeek 原判為 P1，暫不排入任何輪次，其中「圖表頭像標記」`gmgn-website-plan.md` 已提醒「本次沒有實際目視到效果，動筆前建議先在真機確認一次」，這點覆核後同意，動這項之前應該先補測，不要只憑設定頁文字推斷寫進教學。
- **P2 全部（積分商城、邀請返佣、介面設定類、發射台清單、官方圖示定義整頁等）**：同意 DeepSeek 原判不寫，理由不重複列。
- **§2.9 影片素材建議、§2.10 執行時程建議**：這兩節不是「要不要寫進 `index.html`」的規劃範圍，本文件不涉及，留給之後真的要做影片／排時程的人參考。

---

## 給 Opus agent 的執行提醒

- P3-1／P3-2 是**改寫既有文字**，改之前務必先用 Read／Grep 抓到 `index.html` 目前的精確字串再動手（本文件寫的行號僅供參考，實際位置以搜尋結果為準）。
- P3-3 新手風琴請沿用既有 `<details class="acc"><summary>🟣 進階：... ▸</summary>...</details>` 結構與既有手風琴的語氣（例如「Sniper Bot」手風琴），插入位置在「Sniper Bot 與滑點／MEV 三檔設定」（約 627~632 行）之後、「Telegram Alert Bot」（約 634 行起）之前。
- P3-4 請先讀 `⚠️風險` 分頁（`panel-gmgn-risk`，約 690 行起）現有的 callout／自檢清單結構，補在最自然的既有位置（例如既有 callout 列表末尾），不要另開新的 `.card`。
- 全部完成後用 `rtk proxy git diff index.html` 核對只動到這 4 處，並跑一次 HTML 結構完整性檢查（沿用 DEX Screener 那兩輪的驗證方式）。
- 不要 commit、不要 push，交給主 session 統一處理版控。

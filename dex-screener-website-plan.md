# DEX Screener App 功能 → 網站內容規劃（顧問分析）

> 輸入：`dex-screener-features.md`（2026-09-10 實機掃描）
> 對照對象：meme-lol 網站現有的 `#/tools/dexscreener`（🚀上手／🟣進階／📖詞彙／⚠️風險 四分頁架構已存在且內容成熟）
> 前提：目標用戶＝DeFi/Crypto 小白到中級用戶；本規劃只談「App 有、網站還沒講清楚」的缺口，不重複已經做好的部分

## 先講結論

DEX Screener 官網版的核心教學（CA 搜尋防偽、8 個關鍵數字、流動性 vs 市值、Watchlist+Alerts）**網站已經做得很扎實**，不需要重寫。這次 App 實測掃描出來的東西，有價值的缺口集中在 **2 類 App 專屬但網站沒講的內容**：
1. App 的**底部 5 分頁導覽長怎樣**（網站目前寫法偏向網頁版視角，沒告訴讀者「App 打開長什麼樣、5 個按鈕分別幹嘛」）
2. 代幣詳情頁在 App 上其實有 **4 個子分頁（Info/Chart+Txns/Chart/Txns）**，網站目前只講了「交易對頁面的 8 個數字」，沒講清楚這是分頁式介面

**修正說明**：原稿曾把 New 分頁列表上的數字徽章（如「100」）誤判為「安全分數」列為🟢必做項目。事後用 WebSearch 查證＋比對 Search 分頁「熱門推薦」卡片牆上明確帶閃電圖示的「⚡10／⚡50／⚡100」Boost 標記，數字區間完全對得上，判定**這其實是同一種付費推廣 Boost 標記，不是安全分數**——而且網站早就在講這件事（上手 Step 2「Top Boosted 是專案方付錢買的推廣位」＋進階「Boosts 與 Enhanced Token Info 的真相」）。所以這一項從待辦清單移除，改列為「已覆蓋，確認不用動」。

---

## 2.1 功能分類評估

| 等級 | 功能 | 說明 |
|------|------|------|
| 🟢 強烈建議納入 | App 底部 5 大分頁導覽總覽 | 小白第一次打開 App 完全不知道 5 個按鈕在幹嘛，這是「操作說明書」最基本的需求，且網站目前沒有 |
| 🟢 強烈建議納入 | 代幣詳情頁 4 個子分頁（Info/Chart+Txns/Chart/Txns）的用途區分 | 網站現有內容混著講「交易對頁面」，沒告訴讀者其實要點分頁才能看到 K 線/交易紀錄，小白容易卡在 Info 頁以為沒有 K 線 |
| ✅ 已覆蓋，不用動 | New 分頁列表數字徽章（如「100」） | 查證後確認是 Boost 付費推廣標記，非安全分數；網站已有對應內容（上手 Step 2＋進階「Boosts 真相」），不需新增 |
| 🟡 建議納入 | 多鏈搜尋切換（App 內建搜尋框選鏈，支援遠超 8 條鏈） | 對只碰 Robinhood Chain 的目標用戶不是必要，但能避免「明明有的鏈找不到」的困惑 |
| 🟡 建議納入 | Menu 設定頁（主題切換、預設首頁、預設詳情分頁） | 提升體感但非安全/賺錢相關，適合放進「介面設定」這種輔助性 FAQ，不用佔主要教學版面 |
| 🟡 建議納入 | Watchlist 支援多清單（不只一個自選清單） | 中級用戶會想分「觀察中」「已進場」「高風險監控」多個清單，值得一提但不急 |
| 🔴 不建議 / 不適用 | 繪圖工具列（畫線、量測等專業看盤工具） | 技術門檻過高，目標受眾（小白到中級）用不到，且非幣圈特有知識，屬於通用技術分析範疇 |
| 🔴 不建議 / 不適用 | Search 頁「熱門推薦卡片牆（Spotlight）」的完整清單 | 內容每天洗牌、無教學價值，只是入口不是知識點，網站現有「用 CA 搜尋」的教學已經覆蓋更安全的做法 |
| 🔴 不建議 / 不適用 | Alerts 通知權限開通、Menu 裡的 Discord/FAQ/API Reference/法律文件連結 | 純 App 操作瑣事或官方連結，不構成教學內容，網站沒必要重製一份導覽 |
| 🔴 不建議 / 不適用 | DEBUG 分類（App 版本號、User ID） | 開發除錯資訊，讀者完全不需要 |

---

## 2.2 內容模組規劃

### 模組：App 導覽總覽（新增小節，放進「🚀上手」分頁最前面）

- **對應 DEX Screener 功能**：底部 Screener / Search / Watchlist / Alerts / Menu 5 分頁
- **網站實作形式**：教學文章內的一張示意圖 + 條列（沿用現有 `.step` 元件風格）
- **目標用戶**：小白
- **內容大綱**：
  1. 一張「手機截圖示意圖」標出 5 個按鈕位置與白話用途（發現代幣／搜代幣／存追蹤清單／設價格提醒／改設定）
  2. 一句話定位：「日常只會用到 Screener 跟 Watchlist，Search 是備用，Alerts 跟 Menu 隨時可以先跳過」
- **數據來源建議**：手動撰寫（已有本次實測掃描的第一手資料，不需再爬）
- **開發優先級**：P0
- **預估工作量**：小（半天內，1 張新示意圖 SVG + 1 段文案，插入現有 `panel-dexscreener-start` 分頁最前）

### 模組：代幣詳情頁 4 分頁導覽（強化現有「讀懂交易對頁面的 8 個數字」步驟）

- **對應 DEX Screener 功能**：Info / Chart+Txns / Chart / Txns 子分頁
- **網站實作形式**：修改現有 step 3 的教學文字 + 新增一張分頁示意圖
- **目標用戶**：小白～中級
- **內容大綱**：
  1. 講清楚：點進任何一個代幣，預設看到的是 Info（數字總覽），要看 K 線和逐筆交易要點下面另外 3 個分頁
  2. Chart+Txns：K 線 + 下方 Txns/Top Traders/Holders 三合一，中級用戶最常用
  3. Holders 總持有人數是判斷「幣圈參與熱度」的補充指標，可以跟現有「Makers 獨立交易者數」放在一起講
- **數據來源建議**：手動撰寫，沿用本次掃描截圖當示意圖參考（不直接嵌真實截圖，改畫 SVG 示意，符合現站規範）
- **開發優先級**：P0
- **預估工作量**：小～中（修改既有段落 + 1 張新示意圖）

### ✅ 已確認不用做：New Pairs 數字徽章

WebSearch 查證後判定這是 Boost 付費推廣標記（跟 Search 分頁「⚡10／⚡50／⚡100」是同一種東西），非安全分數。網站已有對應內容，**不新增任何段落**。

### 模組：多鏈搜尋切換

- **對應 DEX Screener 功能**：篩選列鏈別 chip → 全螢幕搜尋＋網格選鏈
- **網站實作形式**：現有 step 1「搜尋一定要貼合約地址」段落補一句話
- **目標用戶**：小白（怕找不到自己那條鏈）
- **內容大綱**：
  1. 點鏈別 chip 可以直接打字搜鏈名，不是只能在畫面上的幾個 icon 裡選
- **數據來源建議**：手動撰寫
- **開發優先級**：P2
- **預估工作量**：極小（一句話，不需新圖）

### 模組：Watchlist 多清單

- **對應 DEX Screener 功能**：Watchlist 分頁「Main list」+「+ New」新增清單
- **網站實作形式**：現有 step 5「加自選＋設價格警示」段落補充
- **目標用戶**：中級
- **內容大綱**：
  1. 可以建立多個清單分類管理（例如「觀察中」vs「已進場」），不是只能有一份清單
- **數據來源建議**：手動撰寫
- **開發優先級**：P2
- **預估工作量**：極小

---

## 2.3 網站架構建議（在現有架構內插入，不重建）

meme-lol 目前是單頁 hash 路由（`#/home #/tools #/flow #/glossary #/risk`），DEX Screener 內容掛在 `#/tools` 底下的分頁式子頁面。**不建議另開新的頂層頁面**，本次規劃的內容全部以「強化既有分頁」的方式插入：

```
#/tools（工具頁）
└── DEX Screener（現有 4 分頁，維持架構）
    ├── 🚀 上手
    │   ├── [新增] App 導覽總覽 ← 放最前面
    │   ├── 現有 Step 1～5（CA搜尋／首頁四區塊／8個數字／3個安全判斷／自選+警示）
    │   │       ├── [補一句] Step 1 補上「鏈別 chip 支援打字搜尋」
    │   │       ├── [強化] Step 3 補上「4 分頁導覽」說明
    │   │       └── [補一句] Step 5 補上「Watchlist 支援多清單」
    │   └── 現有三個 metric 卡（流動性／買賣筆數／Pair age+Makers）
    ├── 🟣 進階
    │   └── 現有：Multicharts／New Pairs 篩選器／Boosts 真相（不變動，數字徽章已確認是 Boost，現有內容已覆蓋）
    ├── 📖 詞彙（現有，不變動）
    └── ⚠️ 風險（現有，不變動）
```

---

## 2.4 教學內容微調清單（非全新系列，是既有頁面的補丁）

由於 DEX Screener 教學已存在且完整，這裡不規劃「全新教學系列」，改列**具體修改點清單**：

| # | 位置 | 修改類型 | 摘要 | 預估新增字數 | 需配圖 | SEO/可讀性備註 |
|---|------|---------|------|------------|--------|----------------|
| 1 | 上手／最前面 | 新增段落 | App 5 大分頁一覽 | 約150字 | 需 1 張新 SVG 示意圖 | 讓讀者搜「DEX Screener App 怎麼用」時內容更對得上實際介面 |
| 2 | 上手／Step 3 | 強化既有段落 | 補充「4 分頁才是完整交易對頁」 | 約100字 | 需 1 張新 SVG 示意圖（分頁切換） | 修正讀者對「交易對頁面」的認知落差 |
| 3 | 上手／Step 1 | 補一句話 | 鏈別搜尋支援打字找鏈 | 約30字 | 不需 | — |
| 4 | 上手／Step 5 | 補一句話 | Watchlist 支援多清單 | 約30字 | 不需 | — |

**已從清單移除**：原第 3 項「New Pairs 安全分數徽章 callout」，查證後確認是 Boost 標記、現有內容已覆蓋，不需新增。

**建議發布順序**：1 → 2 → 3/4（1、2 是認知落差最大的缺口，優先做；3、4 是舉手之勞，隨時可以順手加）

**不建議做的事**（呼應 2.1 的🔴）：不用另寫「Search 分頁教學」「Menu 設定教學」「Alerts 通知權限教學」獨立文章——這些是介面瑣事，會稀釕現有內容的「風險判斷」主軸，且會讓頁面更長（HANDOFF.md 已記錄「內容偏長」是現存問題，不宜再加無助於避雷的內容）。

---

## 第二輪分析：Screener 分頁全欄位實測（`dex-screener-screener-tab-walkthrough.md`）

> 輸入：第二輪實機截圖走查（20 個項目，21 張截圖），比第一輪更深入 Screener 首頁的每個篩選欄位
> 判斷原則不變：只挑「小白到中級用戶真的會被這個資訊救到或坑到」的東西，其餘一律跳過，避免頁面繼續變長

### 分級結果

| 等級 | 發現 | 說明 |
|------|------|------|
| 🟢 強烈建議：**修正**既有錯誤內容 | 上手 Step 2「首頁四大區塊」描述跟 App 實際介面對不上 | 現有文案寫「Trending／New Pairs／Gainers & Losers／Top Boosted」四個並列區塊，但實測 App 其實是 **3 顆頂部按鈕（Trending／New／Top）**，Gainers 藏在下拉選單裡，**Losers 甚至不在下拉選單、要點進 Sort 面板的「Price change - Down」才找得到**。這是會讓讀者對著 App 找不到東西的具體錯誤，優先修正 |
| 🟢 強烈建議納入 | Market Cap／FDV／Txns／Buys／Sells 是「數值範圍篩選器」，不是排序 | 可以直接設 Min 門檻濾掉微型市值幣（例如 Market Cap Min $1M），是具體、馬上能用的避雷技巧，且現站完全沒提過這個功能存在 |
| 🟢 強烈建議納入 | Profile 開關的仿冒幣實測對照 | 關掉 Profile 篩選後，「GOOGL」「HOOD」「Claude」這種蹭知名品牌、空白頭像的幣立刻冒出來——這是本次截圖裡最強的「同名仿冒幣」實例，剛好對應風險分頁既有的警告，值得用這個真實案例強化 |
| 🟡 建議納入（可併入現有段落，不必開新區塊） | Boosted／Ads 開關 | 跟現有「Top Boosted 是付費推廣」的警告同一件事的延伸，一句話帶過即可 |
| 🟡 建議納入 | Top 分頁排行榜≠迷因幣熱門榜 | 依量排行時榜首其實是 SOL／RAY／PUMP 這種主流幣，不是迷因幣，容易誤導剛學會用 Top 排序的讀者 |
| 🔴 不建議 | Metas 主題分類、Rank 面板、鏈別/DEX 選擇器細節、Reset 重設範圍 | 功能真實存在但偏進階／內部重複／非避雷相關，寫了只會讓頁面更長，不寫不影響讀者避免虧錢 |

### 建議的 3 個修改點（取代原本待辦清單的優先序）

| # | 位置 | 修改類型 | 摘要 | 預估新增字數 | 需配圖 |
|---|------|---------|------|------------|--------|
| A | 上手／Step 2 | **修正**既有錯誤描述 | 把「首頁四大區塊」改寫成符合實際 App 介面的敘述（3 顆按鈕＋Gainers 藏在下拉＋Losers 藏在 Sort 面板） | 改寫約 80 字，不增加淨字數 | 不需 |
| B | 進階／新增手風琴區塊 | 新增教學 | 「用 Market Cap 篩選器濾掉地雷幣」——教讀者設 Min 門檻 | 約 120 字 | 不需（純文字操作說明） |
| C | 風險或上手／強化既有仿冒幣警告 | 補充實例 | 用「關掉 Profile 開關會冒出 GOOGL/HOOD/Claude 仿冒幣」這個實測結果，具體化既有的「同名仿冒幣」警告 | 約 100 字 | 不需 |

三項合計增加約 220 字（A 是改寫不淨增），比第一輪的規模更小、更聚焦，且 A 是修正錯誤優先於新增。

**不建議做的事**：Boosted/Ads 開關、Top≠熱門榜提醒可以口頭記錄但不建議單獨開段落——已經有現成的 Boost 警告在覆蓋同樣的教育目的，重複寫等於灌水。

---

## 給下一手的提醒

- 本規劃基於 2026-09-10 實機掃描結果（`dex-screener-features.md`），該次掃描的「All DEXes 篩選／Sort 排序選單／幾個 Menu 子設定頁」因裝置操作意外中斷未完整驗證，若要引用這幾項細節做教學內容，建議先補測。
- New Pairs 數字徽章一開始被誤判為「安全分數」，經 WebSearch 查證＋比對 Search 分頁的 Boost 標記後修正為「付費推廣標記，非安全分數」，已從待辦清單移除。這是本次規劃過程中一個具體的查證糾錯案例，留此記錄。
